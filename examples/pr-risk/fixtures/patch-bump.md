# chore(deps): bump axios from 1.6.7 to 1.6.8

Bumps [axios](https://github.com/axios/axios) from 1.6.7 to 1.6.8.

Release notes: fixes a regression in `AxiosHeaders.set` when passed a `Headers`
instance and corrects the TypeScript types for `paramsSerializer`.

No application code changes.
